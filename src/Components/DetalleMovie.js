/* import { urlImgOriginal } from "../Variables Auxiliares/auxiliares"; */

const DetalleMovie = () => {
    return (
        <section>
            <h1>Detalle Movies</h1>
           {/*  <img src={`${urlImgOriginal}${movie.backdrop_path}`} alt={movie.id} key={movie.id}/>
             */}
        </section>
    )
} 

export default DetalleMovie;
